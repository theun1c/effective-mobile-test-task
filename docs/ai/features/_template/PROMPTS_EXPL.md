Изучи AGENTS.md docs/product/TECH_SPEC.md, docs/product/ARCHITECTURE.md, docs/ai/CONTEXT.md и docs/ai/features/
feat-001-subs/FLOW.md.

Реализуй только Шаг 1 из FLOW.md: базовый каркас приложения для сервиса подписок.

Что нужно сделать:

- создать базовый Go-модуль;
- добавить точку входа приложения;
- собрать минимальный app bootstrap;
- добавить загрузку конфигурации из env;
- поднять HTTP router;
- подготовить подключение к PostgreSQL;
- добавить `.env.example`;
- добавить `Dockerfile`;
- добавить `docker-compose.yml`.

Ограничения:

- не реализуй бизнес-логику подписок;
- не добавляй repository/service/handler для subscriptions;
- не делай swagger;
- не реализуй feat-002-sum-cost;
- не меняй архитектуру без причины;
- придерживайся простого монолита `handler -> service -> repository -> db`, но на этом шаге нужен
  только каркас.

Перед изменениями:

1. кратко опиши план;
2. перечисли, какие файлы создашь или изменишь и зачем.

После этого:

- внеси изменения;
- покажи итоговую структуру созданных файлов;
- напиши, как проверить результат локально;
- отдельно перечисли риски или спорные места.

Критерии готовности шага:

- приложение стартует;
- конфиг читается из env;
- есть базовый router;
- есть заготовка подключения к PostgreSQL;
- `docker compose` поднимает app и postgres;
- не добавлено ничего лишнего за пределами Шага 1.
